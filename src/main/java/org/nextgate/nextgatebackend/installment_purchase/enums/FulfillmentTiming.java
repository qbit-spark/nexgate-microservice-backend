package org.nextgate.nextgatebackend.installment_purchase.enums;

public enum FulfillmentTiming {
    IMMEDIATE,        // Ship after down payment
    AFTER_PAYMENT     // Ship after final payment (layaway)
}